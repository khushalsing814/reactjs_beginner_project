import React, { memo } from "react"

function Callback({set_callback,new__count}){
   console.log(`child component`);
    return (
        <>
        

        </>
    )
}

export default memo(Callback)